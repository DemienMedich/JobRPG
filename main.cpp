#include
git checkout
int main(); {
    float version = 0,0,0,1;
    int version = versioncontrol(version);
    cout << "Version: " << version << endl;
    return 0;
}

int versioncontrol(float version) {
    while (version < 1.0) {
        version += 0.1;
    }
    return version;
}